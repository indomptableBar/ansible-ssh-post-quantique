---
- name: Installer OpenSSH Server
  ansible.builtin.dnf:
    name: openssh-server
    state: present

- name: Activer SSHD
  ansible.builtin.systemd:
    name: sshd
    enabled: true
    state: started
